# minimax-m3 (specs/008).
apiVersion: leaderworkerset.x-k8s.io/v1
kind: LeaderWorkerSet
metadata:
  name: minimax-m3
  namespace: fornax
  labels:
    app.kubernetes.io/name: minimax-m3
    app.kubernetes.io/part-of: fornax
spec:
  replicas: 1
  leaderWorkerTemplate:
    size: 1
    restartPolicy: RecreateGroupOnPodRestart
    leaderTemplate:
      metadata:
        labels:
          app.kubernetes.io/name: minimax-m3
      spec:
        nodeSelector:
          latere.ai/gpu-pool: h200
        containers:
          - name: runtime
            image: ghcr.io/latere-ai/fornax-runtime-sglang:v0.1.0
            args: ["serve", "--manifest", "/etc/fornax/model.yaml"]
            ports:
              - containerPort: 8000
                name: http
            readinessProbe:
              httpGet: { path: /readyz, port: 8000 }
              periodSeconds: 10
              failureThreshold: 180 # cold start can be many minutes
            livenessProbe:
              httpGet: { path: /livez, port: 8000 }
              periodSeconds: 30
            resources:
              limits:
                nvidia.com/gpu: "8"
            volumeMounts:
              - { name: model-manifest, mountPath: /etc/fornax }
              - { name: cache, mountPath: /cache }
              - { name: shm, mountPath: /dev/shm }
        volumes:
          - name: model-manifest
            configMap: { name: minimax-m3-manifest }
          - name: cache
            hostPath: { path: /var/cache/fornax, type: DirectoryOrCreate }
          - name: shm
            emptyDir: { medium: Memory, sizeLimit: 32Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: minimax-m3
  namespace: fornax
spec:
  selector:
    app.kubernetes.io/name: minimax-m3
  ports:
    - name: http
      port: 8000
      targetPort: 8000
